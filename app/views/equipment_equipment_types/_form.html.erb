<%= form_with(model: equipment_equipment_type) do |form| %>
  <% if equipment_equipment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipment_equipment_type.errors.count, "error") %> prohibited this equipment_equipment_type from being saved:</h2>

      <ul>
        <% equipment_equipment_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
