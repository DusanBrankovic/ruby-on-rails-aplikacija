<%= render "shared/header" %>

<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card">
        <h3 class="card-header py-3">Kreiraj trening</h3>
        <div class="card-body">
          <%= render "shared/flash" %>
          <%= render "form", workout: @workout, edit: false %>
          <%= link_to 'Back', workouts_path, class: "btn btn-danger" %>
        </div>
      </div>
    </div>
  </div>
</div>

